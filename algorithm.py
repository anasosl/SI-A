import data

def get_frontier(estation):
    

def heuristic_search(e_origin, e_destination):
    o = data.get_number(e_origin)
    d = data.get_number(e_destination)
    fronteiras = []

    fronteiras += get_frontier(o)

    return best_path




heuristic_search.(1, 2)